{
  "name": "imgo-client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.7",
    "typescript": "^5.9.3",
    "vitest": "~3.2.0",
    "wrangler": "^4.61.0"
  }
}
